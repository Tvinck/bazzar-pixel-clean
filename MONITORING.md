# 📊 Система мониторинга и аналитики

В проекте настроена комплексная система для отслеживания ошибок, производительности и поведения пользователей.

## 1. Sentry (Ошибки и производительность)

Используется `@sentry/react`. Инициализация происходит в `src/lib/monitoring.js`.

### Настройка
Добавьте DSN в `.env`:
```
VITE_SENTRY_DSN=https://your-key@sentry.io/project-id
```

### Использование
Ошибки в React компонентах ловятся автоматически через ErrorBoundary. Для ручного отлова ошибок в async функциях:

```javascript
import { captureError } from './lib/monitoring';

try {
  await apiCall();
} catch (error) {
  captureError(error, { context: 'api_call', userId: 123 });
}
```

## 2. Web Vitals (Производительность Frontend)

Метрики (LCP, FID, CLS) собираются автоматически и могут отправляться в Sentry или аналитику.
См. `src/lib/monitoring.js` -> `reportWebVitals`.

## 3. Tracking (Аналитика поведения)

Единая точка входа для событий: `src/lib/tracking.js`.
Позволяет подключить GA4, PostHog, Mixpanel без изменения кода компонентов.

### Использование

```javascript
import { tracking, EVENTS } from './lib/tracking';

// 1. Идентификация пользователя (после логина)
tracking.identify('user_123', { plan: 'premium' });

// 2. Отслеживания событий
tracking.track(EVENTS.GENERATION_STARTED, {
    type: 'image',
    prompt_length: 50
});

// Отслеживание просмотров страниц
tracking.pageView('/home');
```

### Расширение
Для подключения Google Analytics 4, добавьте код инициализации в `tracking.js` внутри метода `init()` и вызовы `gtag` внутри `track()`.

## 4. Структура файлов

- `src/lib/monitoring.js` - Sentry + Web Vitals
- `src/lib/tracking.js` - User Analytics (Events)
- `src/components/ErrorBoundary.jsx` - React Error Boundary

---
**Важно:** В режиме разработки (`npm run dev`) аналитика пишет логи в консоль. В продакшене логи отключены.
