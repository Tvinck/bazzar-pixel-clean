# 🚀 Как протестировать Mini App локально

## Проблема
Telegram Mini Apps требуют HTTPS, но локальный сервер работает на HTTP.

## Решение: Используйте туннель

### Вариант 1: Cloudflare Tunnel (Рекомендую) ✅
```bash
npx cloudflared tunnel --url http://localhost:5174
```
- ✅ Бесплатно
- ✅ Без регистрации
- ✅ Без страницы с паролем
- ✅ Стабильно работает

После запуска скопируйте URL (например: `https://abc-123.trycloudflare.com`)

### Вариант 2: ngrok
1. Зарегистрируйтесь на https://ngrok.com
2. Получите authtoken
3. Запустите:
```bash
./ngrok config add-authtoken YOUR_TOKEN
./ngrok http 5174
```

### Вариант 3: localtunnel (Не рекомендую)
```bash
node tunnel.js
```
⚠️ Показывает страницу с паролем при первом входе

## Настройка бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. `Bot Settings` → `Menu Button` → `Edit Menu Button URL`
5. Вставьте URL из туннеля (например: `https://abc-123.trycloudflare.com`)
6. Готово! Откройте бота и нажмите кнопку меню

## Важно!
- Локальный сервер должен работать: `npm run dev` (порт 5174)
- Туннель должен быть запущен
- При перезапуске туннеля URL может измениться — обновите в BotFather
