# 🎨 PIXEL Mini App: Отчет по улучшениям и Roadmap

## 🚀 Текущее состояние
Приложение успешно запущено с обновленным премиальным дизайном "Space Glass".
- **Стек:** React + Vite + TailwindCSS + Framer Motion.
- **Дизайн:** Светлая тема с элементами темного стекла (Glassmorphism), шум (Noise Texture), крупные параллакс-баннеры.
- **Логотип:** Анимированный пиксельный Grid-логотип с шрифтом Space Grotesk.

---

## 💎 Что можно улучшить (UI/UX & Design)

### 1. **Микровзаимодействия (Micro-interactions)**
- [ ] **Haptic Feedback (Вибрация):** Для Telegram Mini App критически важно добавить тактильный отклик при нажатии кнопок (используя `Telegram.WebApp.HapticFeedback`).
- [ ] **Scroll Animations:** добавить плавное появление элементов при скролле (Fade Up) для длинных списков, чтобы контент не появлялся резко.
- [ ] **Liquid Buttons:** Эффект "жидкого" нажатия на основные кнопки (Create), где границы кнопки немного деформируются.

### 2. **Анимации (Motion Design)**
- [ ] **Page Transitions:** Сделать плавные переходы между табами (Home -> Profile). Сейчас они переключаются мгновенно. Лучше использовать `AnimatePresence` с эффектом сдвига (Slide Over).
- [ ] **Loading States (Скелетоны):** Вместо пустого экрана при загрузке данных показывать красивые "мерцающие" скелетоны карточек.
- [ ] **3D Elements:** Добавить настоящие 3D-объекты (через `react-three-fiber` или `Spline`) на главные баннеры. Например, вращающийся 3D-банан для "Nano Banana".

### 3. **Интерфейс Генерации (Creation Flow)**
- [ ] **Процесс создания:** Сейчас есть кнопки, но нет самого экрана генерации.
    - *Идея:* Сделать всплывающую шторку (Bottom Sheet) на 90% экрана при нажатии на "Image Gen", где сразу будет поле ввода промпта.
- [ ] **Real-time Preview:** Полоса прогресса генерации с меняющимися забавными фразами ("Teaching pixels to dance...", "Mixing colors...").

### 4. **Профиль и Геймификация**
- [ ] **Level System:** Визуализировать уровень пользователя (Novice -> Artist -> Visionary) в зависимости от количества генераций.
- [ ] **Achievements:** Красивые 3D-медали в профиле за достижения.
- [ ] **Dark Mode:** Полноценная темная тема. Текущий дизайн отлично адаптируется инверсией (белый фон -> Black Onyx, черное стекло -> White Glass).

### 5. **Функциональные доработки**
- [ ] **Masonry Layout:** Для ленты "Discover" использовать честную Masonry-сетку (как в Pinterest), чтобы картинки разной высоты вставали плотно без дыр (сейчас используется Grid, который выравнивает по строкам).
- [ ] **Like/Save Animations:** Красивая анимация сердечка (взрыв частиц) при лайке работы в ленте.

---

## 🛠 Технические задачи (Ошибки и Оптимизация)
1. **Шрифты:** Убедиться, что шрифты `Outfit` и `Space Grotesk` загружаются асинхронно и не блокируют рендеринг (FOUT).
2. **Изображения:** Заменить прямые ссылки Unsplash на оптимизированные `webp` варианты или использовать CDN с ресайзом под размер экрана телефона.
3. **PWA / TWA:** Добавить `manifest.json` и мета-теги для корректного отображения как установленного веб-приложения (скрыть браузерный бар Safari/Chrome).
4. **Touch Areas:** Увеличить зоны нажатия у маленьких кнопок (лайки, меню) до рекомендуемых 44x44px.

## 📅 План действий
1. **Фаза 1:** Реализовать переходы между страницами (Router Animations).
2. **Фаза 2:** Верстка экрана генерации (Prompt Input).
3. **Фаза 3:** Подключение реального API генерации (Nano Banana).
