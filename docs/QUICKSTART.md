# 🚀 BAZZAR PIXEL - Быстрый старт

## ✅ Что уже настроено

### 1. **Supabase подключен**
- URL: `https://ktookvpqtmzfccojarwm.supabase.co`
- Anon Key: настроен ✅
- Service Role Key: настроен для бота ✅

### 2. **Telegram Bot**
- Token: настроен ✅
- Web App URL: `https://pixelaibot.ru/` ✅
- Аналитика: включена ✅

### 3. **Приложение**
- Скелетоны загрузки ✅
- Оптимизация изображений (WebP) ✅
- Error Boundaries для 3D ✅
- Звуковые эффекты ✅
- Мультиязычность (RU/EN) ✅

---

## 📋 Следующие шаги

### Шаг 1: Примените SQL схему в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard/project/ktookvpqtmzfccojarwm)
2. Перейдите в **SQL Editor**
3. Создайте **New Query**
4. Скопируйте содержимое файла `supabase_schema.sql`
5. Нажмите **Run**
6. Проверьте, что все таблицы созданы в разделе **Table Editor**

### Шаг 2: Проверьте работу бота

Откройте бота в Telegram и отправьте `/start`

Бот должен:
- ✅ Ответить приветственным сообщением
- ✅ Показать клавиатуру с кнопками
- ✅ Сохранить пользователя в Supabase

### Шаг 3: Проверьте аналитику

После взаимодействия с ботом:

1. Откройте Supabase Dashboard → **Table Editor**
2. Проверьте таблицы:
   - `users` - должен появиться ваш пользователь
   - `user_sessions` - должна быть сессия
   - `events` - должны быть события (клики по кнопкам)
   - `user_stats` - автоматически обновляется

### Шаг 4: Запустите приложение

```bash
npm run dev -- --host
```

Откройте в браузере или через Telegram Web App

---

## 🎯 Что отслеживается

### В боте:
- ✅ Регистрация пользователей (автоматически)
- ✅ Команда `/start`
- ✅ Клики по кнопкам:
  - Сообщество 👥
  - Главное меню 🏠
  - Баланс ⚡
  - Пригласи друга 🤝
  - Трендовые фото 🔥
- ✅ Callback queries (inline кнопки)

### В приложении:
- ✅ Сессии пользователей
- ✅ Telegram user data
- 🔄 Генерации (готово к подключению)
- 🔄 События UI (готово к подключению)

---

## 📊 Просмотр статистики

### Через Supabase Dashboard:

**Активные пользователи сегодня:**
```sql
SELECT COUNT(DISTINCT user_id) as active_today
FROM user_sessions
WHERE created_at >= CURRENT_DATE;
```

**Топ пользователей:**
```sql
SELECT * FROM top_creators LIMIT 10;
```

**Статистика по дням:**
```sql
SELECT * FROM daily_active_users ORDER BY date DESC LIMIT 30;
```

### Через консоль бота:

При запуске бот показывает:
```
🤖 Bot is running...
📊 Total users: 0
✨ Active today: 0
```

---

## 🔧 Полезные команды

### Запуск бота:
```bash
node bot.js
```

### Запуск приложения (dev):
```bash
npm run dev -- --host
```

### Сборка приложения:
```bash
npm run build
```

### Просмотр логов бота:
```bash
# Бот уже запущен в фоне, логи в консоли
```

---

## 📁 Структура проекта

```
bazzar-pixel/
├── src/
│   ├── lib/
│   │   └── supabase.js          # Клиент Supabase для приложения
│   ├── views/
│   │   ├── HomeView.jsx         # Главная с OptimizedImage
│   │   ├── ProfileView.jsx      # Профиль со скелетоном
│   │   └── HistoryView.jsx      # История со скелетоном
│   ├── components/
│   │   ├── ErrorBoundary.jsx    # Обработка ошибок 3D
│   │   └── ui/
│   │       ├── OptimizedImage.jsx  # Оптимизация изображений
│   │       └── Skeletons.jsx       # Скелетоны загрузки
│   └── context/
│       └── SoundContext.jsx     # Звуковые эффекты
├── bot.js                       # Telegram бот с аналитикой
├── bot-supabase.js             # Supabase для бота
├── supabase_schema.sql         # SQL схема БД
├── SUPABASE_SETUP.md           # Подробная документация
└── .env                        # Переменные окружения (не в git)
```

---

## 🎨 Новые фичи

### 1. Оптимизация изображений
- Автоматическое добавление `&fm=webp` к Unsplash
- Blur placeholder при загрузке
- Lazy loading
- Плавные переходы

### 2. Скелетоны загрузки
- ProfileSkeleton - для таба Профиль
- HistorySkeleton - для таба История
- Автоматическая загрузка 1-1.5 сек

### 3. Error Boundaries
- Защита от падения 3D компонентов
- Fallback UI с иконкой
- Логирование ошибок

### 4. Звуковые эффекты
- Клик звук
- Успех звук
- Переключение вкл/выкл в профиле
- Сохранение настроек в localStorage

### 5. Аналитика
- Полное отслеживание действий
- Автоматическое обновление статистики
- Готовые views для дашборда

---

## 🚨 Важно

1. **Не коммитьте `.env`** - он уже в `.gitignore`
2. **Service Role Key** - используйте только на сервере (в `bot-supabase.js`)
3. **Anon Key** - безопасен для клиента, защищен RLS политиками
4. **Примените SQL схему** - без этого аналитика не будет работать

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте, что SQL схема применена
2. Проверьте `.env` файл
3. Проверьте консоль бота на ошибки
4. Проверьте Supabase Dashboard → Logs

---

**Готово к работе!** 🎉

Бот запущен, приложение работает, аналитика настроена.
Осталось только применить SQL схему в Supabase Dashboard.
