# 🏗 АРХИТЕКТУРА ПРОЕКТА

## 📊 Текущая структура

```
┌─────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ                              │
└────────────┬────────────────────────────┬────────────────────┘
             │                            │
             ▼                            ▼
    ┌────────────────┐          ┌────────────────┐
    │  TELEGRAM BOT  │          │   MINI APP     │
    │   (Node.js)    │          │   (React)      │
    └────────┬───────┘          └────────┬───────┘
             │                           │
             │  bot-supabase.js         │  src/lib/supabase.js
             │                           │
             └───────────┬───────────────┘
                         ▼
              ┌──────────────────────┐
              │     SUPABASE         │
              │  ┌────────────────┐  │
              │  │  PostgreSQL    │  │
              │  │  - users       │  │
              │  │  - sessions    │  │
              │  │  - events      │  │
              │  │  - stats       │  │
              │  └────────────────┘  │
              │  ┌────────────────┐  │
              │  │    Storage     │  │
              │  │  - images      │  │
              │  │  - videos      │  │
              │  └────────────────┘  │
              └──────────────────────┘
```

---

## 🔄 Поток данных

### 1. Регистрация пользователя:
```
Telegram → Bot → botAnalytics.upsertUser() → Supabase users
                                            → Trigger → user_stats (balance: 100)
```

### 2. Генерация контента:
```
User → Bot/MiniApp → AI API → Result → Supabase Storage
                                      → Supabase creations
                                      → Trigger → user_stats (total_generations++)
```

### 3. Оплата:
```
User → Payment Button → YooKassa → Webhook → Server
                                            → Supabase transactions
                                            → user_stats (balance += amount)
```

---

## 📁 Структура файлов

```
bazzar-pixel/
│
├── 📱 BOT
│   ├── bot.js                    # Основной файл бота
│   ├── bot-supabase.js          # Supabase для бота
│   └── .env                     # Переменные окружения
│
├── 🎨 MINI APP
│   ├── src/
│   │   ├── App.jsx              # Главный компонент
│   │   ├── views/               # Страницы
│   │   │   ├── HomeView.jsx
│   │   │   ├── ProfileView.jsx
│   │   │   ├── HistoryView.jsx
│   │   │   └── SearchTab.jsx
│   │   ├── components/          # Компоненты
│   │   │   ├── CreationDrawer.jsx
│   │   │   ├── Onboarding.jsx
│   │   │   ├── ErrorBoundary.jsx
│   │   │   ├── 3d/
│   │   │   │   ├── AbstractCore.jsx
│   │   │   │   └── Medal3D.jsx
│   │   │   └── ui/
│   │   │       ├── OptimizedImage.jsx
│   │   │       ├── Skeletons.jsx
│   │   │       ├── ToolCard.jsx
│   │   │       ├── LikeButton.jsx
│   │   │       └── BalanceDisplay.jsx
│   │   ├── context/             # Контексты
│   │   │   ├── LanguageContext.jsx
│   │   │   └── SoundContext.jsx
│   │   ├── lib/                 # Утилиты
│   │   │   └── supabase.js      # Supabase клиент
│   │   └── utils/
│   │       └── translations.js   # Переводы
│   │
│   ├── public/                  # Статика
│   ├── dist/                    # Сборка
│   └── package.json
│
├── 🗄 DATABASE
│   └── supabase_schema.sql      # SQL схема
│
└── 📚 DOCS
    ├── ROADMAP.md               # План развития
    ├── TODO.md                  # Краткий план
    ├── CHECKLIST.md             # Чеклист готовности
    ├── QUICKSTART.md            # Быстрый старт
    ├── SUPABASE_SETUP.md        # Настройка Supabase
    ├── DATA_SYNC.md             # Синхронизация данных
    └── ARCHITECTURE.md          # Этот файл
```

---

## 🔌 API Endpoints (будущее)

### Supabase Functions:
```
POST /functions/v1/generate-image
POST /functions/v1/generate-video
POST /functions/v1/process-payment
GET  /functions/v1/user-stats
```

### Webhook'и:
```
POST /webhooks/yookassa        # Обработка платежей
POST /webhooks/telegram        # Telegram updates
```

---

## 🎯 Компоненты системы

### 1. Telegram Bot (bot.js)
**Отвечает за:**
- Обработку команд (/start, /help)
- Обработку сообщений (фото, текст)
- Кнопки меню
- Inline кнопки
- Отправку результатов

**Использует:**
- `node-telegram-bot-api`
- `bot-supabase.js` для БД
- `.env` для конфигурации

### 2. Mini App (React)
**Отвечает за:**
- UI/UX интерфейс
- Форма генерации
- Профиль пользователя
- История генераций
- Галерея работ

**Использует:**
- React + Vite
- Framer Motion (анимации)
- React Three Fiber (3D)
- Supabase JS Client

### 3. Supabase Backend
**Отвечает за:**
- Хранение данных
- Аутентификация
- Storage для медиа
- Realtime обновления
- Edge Functions

**Таблицы:**
- `users` - пользователи
- `user_sessions` - сессии
- `user_stats` - статистика
- `events` - события
- `generations` - генерации
- `creations` - сохраненные работы
- `transactions` - платежи
- `referrals` - рефералы

---

## 🔐 Безопасность

### Row Level Security (RLS):
```sql
-- Пользователи видят только свои данные
CREATE POLICY "Users can view own data" ON users
    FOR SELECT USING (auth.uid() = id);

-- Публичные креации видны всем
CREATE POLICY "Public creations viewable" ON creations
    FOR SELECT USING (is_public = true);
```

### API Keys:
- `VITE_SUPABASE_ANON_KEY` - для клиента (безопасно)
- `SUPABASE_SERVICE_ROLE_KEY` - для бота (секретно!)
- `TELEGRAM_BOT_TOKEN` - для бота (секретно!)

---

## 📈 Масштабирование

### Текущая нагрузка:
- 1-100 пользователей ✅
- 1-1000 генераций/день ✅
- Supabase Free Tier ✅

### При росте до 1000+ пользователей:
- Переход на Supabase Pro
- Добавить Redis для кеша
- CDN для статики
- Load balancer

### При росте до 10000+ пользователей:
- Микросервисная архитектура
- Отдельный сервер для AI
- Очередь генераций (RabbitMQ/Redis)
- Horizontal scaling

---

## 🛠 Технологический стек

### Frontend:
- **React** - UI фреймворк
- **Vite** - сборщик
- **Framer Motion** - анимации
- **React Three Fiber** - 3D
- **Lucide React** - иконки
- **TailwindCSS** - стили (в будущем)

### Backend:
- **Node.js** - бот
- **Supabase** - БД и бэкенд
- **PostgreSQL** - база данных
- **Supabase Storage** - файлы

### DevOps:
- **Git** - версионирование
- **npm** - пакеты
- **Vercel/Netlify** - хостинг фронта
- **Railway/Render** - хостинг бота

### AI/ML:
- **Replicate** - AI API (рекомендуется)
- **Stability AI** - альтернатива
- **OpenAI DALL-E** - альтернатива

---

## 🔄 CI/CD Pipeline (будущее)

```
Git Push → GitHub Actions → Tests → Build → Deploy
                                           ├─→ Vercel (Mini App)
                                           └─→ Railway (Bot)
```

---

## 📊 Мониторинг (будущее)

### Метрики:
- **Uptime** - доступность сервиса
- **Response Time** - скорость ответа
- **Error Rate** - процент ошибок
- **Active Users** - активные пользователи

### Инструменты:
- **Sentry** - отслеживание ошибок
- **Amplitude** - аналитика пользователей
- **Supabase Dashboard** - метрики БД
- **Telegram Bot Analytics** - статистика бота

---

## 🎨 Design System (будущее)

### Цвета:
```css
--primary: #8B5CF6      /* Violet */
--secondary: #EC4899    /* Pink */
--success: #10B981      /* Green */
--warning: #F59E0B      /* Amber */
--error: #EF4444        /* Red */
```

### Компоненты:
- Buttons (primary, secondary, ghost)
- Cards (default, glass, gradient)
- Inputs (text, textarea, select)
- Modals (drawer, dialog, sheet)
- Skeletons (card, list, profile)

---

**Архитектура готова к масштабированию!** 🚀
