# 🚀 Инструкция по запуску (Bazzar Pixel v2.0)

Проект полностью обновлен. Исправлены критические баги, повышена безопасность и добавлен функционал роста (рефералка).

## 1. База Данных (Supabase)
Вам нужно выполнить SQL запросы, чтобы активировать новые функции.
Перейдите в **Supabase Dashboard -> SQL Editor**, создайте новый запрос и по очереди выполните содержимое следующих файлов:

### А. Функция возврата средств (Авто-рефанд)
Используйте код из файла: `migrations/23_refund_function.sql`
> *Это позволит боту возвращать кредиты пользователям при ошибках генерации.*

### Б. Реферальная система
Используйте код из файла: `migrations/24_referral_system.sql`
> *Это добавит таблицу рефералов и возможность начислять бонусы за приглашения.*

---

## 2. Настройка Environment (.env)
Для работы **Очереди Задач** (чтобы генерации не терялись при перезагрузке) нужно прямое подключение к базе.
Добавьте в ваш `.env` файл на сервере:

```ini
# Строка подключения для PgBoss (Queue). 
# Скопируйте из Supabase: Settings -> Database -> Connection String -> URI -> Mode: Transaction Session (Port 5432)
DATABASE_URL="postgres://postgres:[ВАШ-ПАРОЛЬ]@db.[ВАШ-ПРОЕКТ].supabase.co:5432/postgres"
```

Если вы не добавите `DATABASE_URL`, бот будет работать в **синхронном режиме** (как раньше). Это ок для тестов, но для продакшена лучше добавить.

---

## 3. Проверка Функционала

### ✅ Видео (Kling Motion)
1. Откройте Web App.
2. Выберите модель **Kling Motion Control**.
3. Убедитесь, что появились **два** поля загрузки: "Фото персонажа" и "Видео движения".
4. Загрузите файлы и нажмите "Genererate".

### ✅ Рефералка
1. Возьмите свой ID (например, `12345`).
2. Отправьте другу ссылку: `https://t.me/ВашБот?start=r-12345`.
3. Когда друг нажмет Start, вам должно прийти уведомление о бонусе (после того как вы примените SQL миграцию).

### ✅ История
1. Зайдите в Профиль -> Аккаунт.
2. Проверьте список "История транзакций".

---

## Что делать дальше?
Проект технически готов к масштабированию.
- Замените временные картинки-превью в `src/config/models.js` на ваши реальные красивые генерации.
- Настройте маркетинговые тексты в `bot.js` (переменная `welcomeMessage`).

🚀 **Успешного запуска!**
